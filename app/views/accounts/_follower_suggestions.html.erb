<% @follower_suggestions.each do |user| %>
  <div class="recommended-follower">
    <%= image_tag user.image.thumb.url, class: "profile-pic" if user.image.present? %>
    <%= user.username %>
  </div>
<% end %>